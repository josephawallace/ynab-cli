# Security policy

Report vulnerabilities through this repository's private vulnerability-reporting channel. Do not include an access token, account data, or a reproduction against a real YNAB account. Reports receive an acknowledgement within seven days.

If a token is exposed, revoke it in YNAB immediately and replace it through an approved runtime credential source.
